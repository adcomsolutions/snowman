export const uponLoad = (fn) => {
  onLoad = fn
}

export const uponChange = (fn) => {
  onChange = fn
}

export const uponSubmit = (fn) => {
  onSubmit = fn
}

export const uponCellEdit = (fn) => {
  onCellEdit = fn
}
